# Node.jsの公式イメージを使用
FROM node:18

# 作業ディレクトリを作成
WORKDIR /app

# package.jsonとlockファイルをコピー
COPY package*.json ./

# 依存関係をインストール
RUN npm install

# アプリのコードをコピー
COPY . .

# ポート3000を開ける（Next.jsのデフォルト）
EXPOSE 3000

# 開発サーバーを起動
CMD ["npm", "run", "dev"]
